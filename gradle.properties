# Gradle 使用的 JDK 根目录。这里必须指向真正的 JDK 根目录，不能填写 bin、Packages 等子目录。
# Windows 路径使用正斜杠，避免 properties 文件中的反斜杠转义问题。
# 如果其他开发机的 JDK 安装位置不同，应在本机用户级 ~/.gradle/gradle.properties 中覆盖此配置。
org.gradle.java.home=F:/Java/Jdk21

# 统一使用 UTF-8，避免中文注释、插件描述和编译输出出现乱码。
systemProp.file.encoding=UTF-8
